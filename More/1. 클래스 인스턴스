* 클래스를 통해 생성한 인스턴스에 대하여

- 클래스를 통해 생성한 인스턴스는 참조변수로만 쓸 수 있다. 
- '클래스명 변수명;' 을 통해클래스 객체를 참조변수가 참조하게 되고, '변수명 = new 클래스명();' 을 통해 인스턴스가 생성된 후 참조변수에 이 인스턴스의 주소가 저장된다. 
- 클래스명 변수명; 선언을 통해 메모리에 변수를 위한 공간이 생성된다. 
- new 클래스명()을 통해 인스턴스가 생성이 되면서 어떤 메모리 주소에 멤버변수나 메소드들이 저장된다. 
- 이후 지정연산자를 통해 이 인스턴스의 메모리 주소를 변수가 참조하게 된다. 

